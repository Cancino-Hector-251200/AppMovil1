<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CitaMedica">
        <activity
            android:name=".presentation.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CitaMedica">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.activity.RegisActivity"
            android:exported="false"
            android:label="Registro"
            android:theme="@style/Theme.CitaMedica" />
        <activity
            android:name=".presentation.activity.WelcomeActivity"
            android:exported="false"
            android:label="Bienvenida"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CitaMedica" />
        <activity
            android:name=".presentation.activity.PerfilActivity"
            android:exported="false"
            android:label="Perfil"
            android:theme="@style/Theme.CitaMedica" />
        <activity
            android:name=".presentation.activity.DoctorActivity"
            android:exported="false"
            android:label="Doctores"
            android:theme="@style/Theme.CitaMedica" />
        <activity
            android:name=".presentation.activity.AgendarCitaActivity"
            android:exported="false"
            android:label="Agendar Cita"
            android:theme="@style/Theme.CitaMedica" />
        <activity
            android:name=".presentation.activity.HorariosActivity"
            android:exported="false"
            android:label="Horarios"
            android:theme="@style/Theme.CitaMedica" />

        <service
            android:name=".presentation.service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>